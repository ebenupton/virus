MEMORY {
    GRIDBUF: start = $0200, size = $00BD, file = "";
    BUFFERS: start = $02BD, size = $006E, file = "";
    CODE: start = $032B, size = $28D5, file = %O, fill = yes;
    HEIGHTMAP: start = $2C00, size = $0400, file = %O;
}
SEGMENTS {
    GRIDBUF: load = GRIDBUF, type = bss;
    BUFFERS: load = BUFFERS, type = bss;
    CODE: load = CODE, type = rw;
    HEIGHTMAP: load = HEIGHTMAP, type = rw;
}
