MEMORY {
    BUFFERS: start = $0300, size = $0300, file = "";
    CODE: start = $0600, size = $2A00, file = %O;
}
SEGMENTS {
    BUFFERS: load = BUFFERS, type = bss;
    CODE: load = CODE, type = rw;
}
