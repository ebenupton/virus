MEMORY {
    ZP:   start = $0000, size = $0100, type = rw, define = yes;
    CODE: start = $0200, size = $FDE0, file = %O;
}

SEGMENTS {
    ZEROPAGE: load = ZP,   type = zp;
    CODE:     load = CODE, type = rw;
    RODATA:   load = CODE, type = ro;
}
